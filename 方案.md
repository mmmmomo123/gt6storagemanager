# GT6 附属 Mod「储物桶管理器」最终方案

> **实现状态：✅ 完整源码工程已生成**，位于本目录（`gt6storagemanager/`）。
> 5 个 Java 类 + 构建脚本 + 中英文 lang + 占位贴图，所有 API 签名已对照 GT6 6.17.06 官方源码与 jar 字节码核实。
> 构建方式见 [README.md](README.md)（本机未编译，按约定只出源码）。

> 目标：Minecraft 1.7.10 + GregTech 6（开发目标版本 6.17.06）
> 类型：GT6 附属 mod（依赖 `gregapi_post`）
> 定位：对标 Storage Drawers 的「抽屉管理器」，但管理对象是 GT6 储物桶家族；**无 GUI**，只做隐形自动化接口 + 右键存入。

---

## 一、环境与构建

- 本机（Ubuntu 24.04）：`/var/cache/apt` 只读，无法 apt 装 JDK；但可下载 **Temurin JDK 8** 压缩包到工作区解压使用（1.7.10 必须用 Java 8）。
- Forge MDK 已下载：`moddev/forge`（MinecraftForge 1.7.10-10.13.4.1614）。
- GT6 官方源码参考：https://github.com/GregTech6/gregtech6
- 构建：Gradle 2.14.1 wrapper + ForgeGradle 1.2；`setupDecompWorkspace` → `build`。
- GT6 依赖：`libs/gregtech_1.7.10-6.17.06.jar`，以 `provided` 方式引入；编译期凡涉及原版方法一律先强转 `IInventory`（MCP 名），GT 自家方法（`insertItems` 等不混淆名）直接调用。这是 1.7.10 编译附属的常规做法。
- 测试：`runClient`，把 GT6 release jar 丢进 `run/mods`，摆桶验证。

---

## 二、已验证的 GT6 API（来自所提供 jar 的字节码 + 官方源码）

| 事实 | 说明 |
|---|---|
| 储物桶家族 | `gregtech.tileentity.inventories.MultiTileEntityMassStorageBox` / `...Standard` / `...Barrel` / `...Logistics`，全部继承 `gregapi.tileentity.inventories.MultiTileEntityMassStorage` |
| 实现原版 IInventory | 经 `gregapi.tileentity.base.TileEntityBase05Inventories implements net.minecraft.inventory.IInventory`，`getStackInSlot / decrStackSize / setInventorySlotContents / markDirty` 均可用 |
| 槽位语义 | `slot 0` = 输入槽（放入即经 `OM.get()` 矿物词典统一后转入存储）；`slot 1` = 主存储，单一品种，上限 `mMaxStorage`（默认 100 万，大容量桶可到 21 亿） |
| `ItemStack insertItems(ItemStack, boolean)` | 官方插入接口，返回剩余；胶带锁定（`mMode` bit 3）时自动拒绝 |
| `int getMaxContent()` / `int mMaxStorage` | 容量 |
| `long mPartialUnits` | 零头数量（粉/锭单位以下），`getPartialStack()` 可换算成对应物品 |
| `byte mMode` | bit 3 = 胶带锁定（抽取前需自查） |
| 不实现 ISidedInventory | GT 用自家 `getAccessibleSlotsFromSide2` 侧面系统，所以管理器必须自己实现 `ISidedInventory` 才能被 BC/AE2 识别 |

---

## 三、设计规格（已定稿）

### 3.1 方块与 TE

- modid：`gtsm`；方块显示名：`储物桶管理器`（`Storage Manager`）；材质机器风（1 张方块贴图 + 破坏粒子等）。
- 注册方式（GT6 官方 Example 套路）：
  - `StorageManager_Mod extends gregapi.api.Abstract_Mod`，`@Mod(modid="gtsm", name="储物桶管理器", version="1.0", dependencies="required-after:gregapi_post")`
  - `@SidedProxy(clientSide="gtsm.Proxy_Client", serverSide="gtsm.Proxy_Server")` + 两个 `Abstract_Proxy`
  - `MultiTileEntityBlock.getOrCreate("gtsm","machine", MaterialMachines.instance, soundTypeMetal, TOOL_wrench, ...)`
  - `MultiTileEntityRegistry.getRegistry("gtsm.multitileentity").add("储物桶管理器", "Storage", 1, 0, TileEntityStorageManager.class, 16, tMachineBlock, NBT, 合成表)`
- 合成：GT 风格（如 4 机器外壳 + 1 物品管道 + 1 电路），用 `tRegistry.add` 自带配方。

### 3.2 扫描与桶列表

- 识别条件：`te instanceof gregapi.tileentity.inventories.MultiTileEntityMassStorage`。
- 范围：以管理器为中心的立方体，半径 `R`，**配置可调 1~8，默认 1**（半径 1 = 3×3×3 去中心 = 26 个位置）。
- 上限：**26 个桶**（半径 1 恰好填满）；超出部分忽略并在日志/F3 提示。
- 刷新策略：缓存桶引用；方块邻接变化（`updateEntity`/`onNeighborChange`）时标记重扫；兜底每 64 tick 检查一次桶是否仍有效（未卸载、未破坏）。避免每 tick 全扫。

### 3.3 幻影视图 IInventory（核心）

| 槽 | 行为 |
|---|---|
| 槽 0 = **通用路由槽** | 插入任意物品：① 已有同品种桶 → 走该桶 **slot 0**（矿物词典统一，`OM.get`）；② 无匹配且允许填充空桶 → 找空桶走其 slot 0；③ 都没有 → `canInsertItem(0)` 返回 false |
| 槽 1..N = **幻影槽** | 一一对应扫描到的桶 |

实现要点：
```java
int     getSizeInventory()        { return Math.min(boxes.size(), 26) + 1; }
ItemStack getStackInSlot(int i)    { return box(i)==null ? null : ST.copy(box(i).getStackInSlot(1)); }
ItemStack decrStackSize(int i, int n) {
    return i==0 ? null : (isLocked(box(i)) ? null : box(i).decrStackSize(1, n));
}
void setInventorySlotContents(int i, ItemStack s) {
    if (i==0) routeInsert(s);           // 路由到匹配桶/空桶（走桶 slot 0）
    else if (!isLocked(box(i))) box(i).setInventorySlotContents(0, s); // 桶 slot0 自动统一
}
ItemStack getStackInSlotOnClosing(int i) { return null; }   // 无真实库存
int  getInventoryStackLimit()      { return 64; }           // 幻影槽保持常规上限
boolean isItemValidForSlot(int i, ItemStack s) {
    if (i==0) return true;              // 路由槽收一切
    MultiTileEntityMassStorage b = box(i);
    if (b==null || isLocked(b)) return false;
    return !b.slotHas(1) || ST.equal(s, b.slot(1));
}
void markDirty() { /* no-op，桶自身负责 */ }
```

- 矿物词典统一（已定稿）：一律走桶 `slot 0`（`setInventorySlotContents(0, s)` 内部 `insertItems(OM.get(s), F)`）。
- 空桶自动填充（已定稿）：路由槽在无匹配品种时占用空桶。
- 胶带锁定：插入 `insertItems` 自带拒绝；抽取侧自查 `mMode & B[3]`。

### 3.4 ISidedInventory（自动化兼容）

- 六面全开放；`getAccessibleSlotsFromSide` 返回全部槽；`canInsertItem/ExtractItem` 委托 `isItemValidForSlot` + 锁定判定。
- （可选二期）扳手/螺丝刀切换开放面，贴合 GT6 交互习惯。

### 3.5 玩家交互

- **右键**（手持物品）：手里的物品走通用路由槽逻辑插入；成功 → 挥手 + 桶操作音效。
- **潜行右键**：尝试插入玩家背包内全部同品种堆叠（方便倒货）。
- 放大镜（GT 工具）查看：管理范围内桶数量 / 已占用 / 锁定数。
- 软锤：清空路由槽残留。螺丝刀：切换"是否填充空桶"。
- 无 GUI、无 Container、无需网络包（幻影视图服务端实时计算，客户端自动化不读槽）。

### 3.6 配置（`config/gtsm.cfg`）

```ini
general {
    I:scanRadius=1          # 1~8
    I:maxBoxes=26
    B:fillEmptyBoxes=true   # 自动占用空桶装新品种
    B:oreDictUnify=true     # 矿物词典统一（走桶 slot0）
    B:enableLocker=false    # 预留：是否纳入 MultiTileEntityLocker
}
```

### 3.7 本地化

- `assets/gtsm/lang/zh_cn.lang`：`tile.gtsm.multitileentity.1.name=储物桶管理器`
- `assets/gtsm/lang/en_us.lang`：`Storage Manager`
- tooltip 提示扫描半径与桶数。

---

## 四、项目结构（已实现）

```
gt6storagemanager/
├── build.gradle                    # ForgeGradle 1.2 / MC 1.7.10 / Forge 10.13.4.1614
├── gradle/wrapper/                 # Gradle 2.14.1 wrapper
├── libs/gregtech_1.7.10-6.17.06.jar # 编译期依赖（provided，不打包）
└── src/main/
    ├── java/gtsm/
    │   ├── StorageManager_Mod.java          # extends Abstract_Mod + @Mod + 事件转发 + 方块注册
    │   ├── Proxy_Client/Proxy_Server.java   # extends Abstract_Proxy
    │   ├── Config.java                      # config/gtsm.cfg 读取的配置值
    │   └── tile/TileEntityStorageManager.java   # 核心：幻影视图 + 扫描路由 + 右键/工具交互 + 渲染
    └── resources/
        ├── mcmod.info                       # 依赖 gregapi_post
        └── assets/gtsm/{lang,textures/blocks}/  # zh_cn/en_us + 占位贴图
```

实际 5 个类、约 900 行代码。核心逻辑（`TileEntityStorageManager`）实现要点：

- **槽位映射**：槽 0 = 通用路由槽（先匹配已有品种桶 → 可选填充空桶），槽 1..26 = 储物桶幻影槽
- **幻影视图**：`getStackInSlot(i)` 返回 `box.slot(1)` 的拷贝；`decrStackSize(i,n)` 转发 `((IInventory)box).decrStackSize(1,n)`；`setInventorySlotContents` 插入失败时回退路由，**零物品丢失**
- **插入**：`box.insertItems(OM.get(copy), F)` —— 矿物词典统一 + 胶带锁定自动拒绝（insertItems 内部判定）
- **抽出**：`((IInventory)box).decrStackSize(1,n)`，抽出前自查 `mMode & B[3]` 拒绝胶带锁定桶
- **自动化**：`IInventory` + `ISidedInventory` 六面全开放
- **交互**：右键/潜行右键存入；螺丝刀切换填充空桶、软锤重扫、放大镜查看详情；比较器输出 0~15
- **贴图**：自建 `IIconContainer`（实现 `Runnable` 注册到 `GT_API.sBlockIconload`），方块渲染用 `BlockTextureDefault.get`

---

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| GT6 版本更新改 API | 锁定 6.17.06；交互只用 `IInventory` + `slot 0` + `insertItems` 稳定接口；GT 类引用集中在 `BoxHelper` |
| 数量 > int 显示 | 本版无 GUI，幻影槽 stackSize 本身是 int（上限 21 亿够用）；零头留桶内 |
| 满桶/锁定导致物品丢失 | `canInsertItem` 在不可插入时返回 false；本 mod 无缓存层，不暂存任何物品 → 零丢失 |
| 扫描性能 | 懒刷新 + 缓存 + 兜底定时检查 |
| 幻影槽 stackSize 巨大（如 100 万）惊到某些 mod | `getInventoryStackLimit()` 返回 64 约束插入逻辑；抽取走 `decrStackSize` 分批 |
| 美术资源 | 方块贴图 + 破坏粒子；先出占位图 |
| 空桶被误占用 | 配置 `fillEmptyBoxes` 可关；路由槽设计使自动化只在无匹配时才动空桶 |

---

## 六、构建步骤（给你的电脑）

```bash
# 1. 装 Java 8（Temurin 8），确认 java -version 是 1.8
# 2. 把 gt6storagemanager/ 拷到本机
# 3. 放入 GT6 jar 到 libs/
# 4. 构建
./gradlew setupDecompWorkspace --refresh-dependencies   # 首次
./gradlew build                                          # 产物 build/libs/gtsm-1.0.jar
# 5. 测试：./gradlew runClient，把 GT6 jar 放 run/mods，进游戏摆桶
```

---

## 七、二期可选扩展（先不做）

- GUI 汇总面板（每桶品种/数量/进度条）
- 管理器 + 从机多方块网络（SD 控制器/从机架构）
- 纳入 Locker / 通用 IInventory
- 主动推送模式（管理器自己往桶里塞，而非被动响应自动化）
