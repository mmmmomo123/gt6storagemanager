# GTSM 更新日志 / 问题修复记录

> 汇总 v1.0.0 → v1.0.9 开发过程中遇到的全部问题、根因与修复方式。
> 只保留 v1.0.9 一个发布版（GitHub Releases），jar 名带版本号。

## v1.0.9 — 多管理器重复计数（最终问题）
- **现象**：2 个储物桶管理器同时被 AE2 存储总线读取时，物品数量×管理器数量。
- **根因**：多个管理器扫描范围重叠，**同一个储物桶被多方认领**，AE2 网络重复计数。
- **修复**：每个储物桶只归「最近的管理器」所有（距离近者赢；距离相同按坐标字典序，保证多个管理器判定一致、无歧义）。多管理器可用 GUI 偏移量错开范围，分管不同区域。

## v1.0.8 — 重进游戏方块隐形（真凶）+ AE2 数量虚高
- **隐形真凶**：客户端 TE 从未创建。日志证据：`TE loaded at (718,4,-1011)` 只在 `[Server thread]` 出现，客户端一次都没有 → 无 TE → 不渲染、不能右键（但碰撞还在、服务端功能正常）。根因：**我们自己把 `getClientDataPacket()` 重写为返回 null** —— GT6 用这个包（PacketSyncDataIDs：注册表 id + mTE id）向客户端同步 TE 身份，被掐断。新放置时走方块更新包所以当场能看到，重进游戏才暴露。
- **修复 1**：删除该重写，恢复 GT6 默认同步。
- **AE2 虚高 1→3**：同一物品有**两条写入路径**（路由槽 0 与幻影槽 1..N 都能收），AE2 写两次 → 数量虚高。
- **修复 2**：**幻影槽只读**（纯展示视图），路由槽(0)唯一写入入口；抽取（decrStackSize）不受影响。

## v1.0.7 — AE2 小撮/粒写入计数错乱
- **根因**：`setInventorySlotContents` 差值语义假设“同品种”，写入不同单位物品时按裸数量算差值（36<100 误触发“抽取”）。
- **修复**：先判同品种（ST.equal）——同品种才走数量差值；不同品种/单位一律走 GT6 打包插入。

## v1.0.6 — AE2 无法打包小撮/粒
- **根因**：`isItemValidForSlot` 用 `ST.equal` 死板判断（只有同品种才准入），可打包的小单位被挡在 AE2 门外。
- **修复**：改用 GT6 储物桶官方 `allowInsertion()`：同品种 YES、可打包小单位（小撮→粉碎矿石、粒→锭）YES、胶带锁定 NO。

## v1.0.5 — v1.0.4 启动崩溃热修
- 崩溃：`IllegalStateException: Blocks can only be initialised within preInit!`（方块创建被放进了 Init 阶段）
- 修复：方块创建移回 preInit。

## v1.0.4 — 元数据碰撞（ GT6 机器块尝试失败）
- v1.0.2/v1.0.3 曾把 MTE 注册到 GT6 机器块上图 ID 稳定，但我们的 block 元数据 0 与 GT6 自家机器撞车（重进游戏被当成 GT6 机器 → 不渲染、不能交互）。
- 修复：回退使用本 mod 自有方块。

## v1.0.3 — 图标只注册一次（隐形但有碰撞）
- **根因**：1.7.10 每次进游戏都**重建方块图集**，旧实现用 `GT_API.sBlockIconload` 一次性队列（每 JVM 只入队一次），重载后图标引用失效 → 方块隐形但有碰撞（看起来像空气，其实世界数据一直在）。
- **修复**：`TextureStitchEvent.Pre` 每次缝合重新注册 + TE 实现 `IMTE_RegisterIcons` 双保险。

## v1.0.0~v1.0.2 — 奠基与核心 bug
- **AE2 读取后储物桶不减少、还能凭空取出（复制物品）**：AE2 rv3 的 `AdaptorIInventory.removeItems` **不调用 decrStackSize**，而是 `setInventorySlotContents(slot, 剩余堆)` 写回扣除；原实现把剩余堆当“要放入的物品”又塞回桶里（1000 取 64 → 写回 936 → 桶变 1936）。**修复：差值语义**（target<当前→取差值；target>当前→放差值）。
- **往 AE 放物品虚无消失**：路由槽兜底丢弃剩余。**修复：准入门槛 + 剩余掉落成实体**。
- **编译期**：ForgeGradle 1.2 写死的 Mojang S3 地址早已 404 → URL 修补到 piston-meta/launcher.mojang.com；GT6 继承链引用的 AE2/IC2/BC/CoFH/NEI/Mekanism API → 空桩（不打进 jar）；`getExplosionResistance2`→`getExplosionResistance`、补全 Abstract_Mod 抽象方法等。
- **界面**：圆角深色卡片、胶囊步进器、拨动开关；修悬停高亮错位（绘制绝对坐标 vs 命中相对坐标混用）；范围画框改用 TESR（Angelica 渲染管线会改变世界事件矩阵语义，TESR 是稳定契约）。
- **汉化**：GUI 硬编码中文 + `IMTE_GetItemName` 代码直返中文名 + en_us/zh_cn 双 locale + LH.add 默认值中文。
- **配方**：铝板×4 + t3以上电路×4（高级/精英/大师/究极）+ 铂物品管道×2 + 存储输入器。
- **多管理器**：幻影槽只读 + 路由槽单写入口（v1.0.8）+ 归属最近管理器（v1.0.9）。

## 经验备忘（给后续维护）
- **幻影库存三原则**：写入路径唯一（单一路由槽）；差值语义要判物品一致性；多实例要防重复认领。
- **GT6 MTE 生存指南**：方块只能在 preInit 建；TE 身份同步靠 getClientDataPacket（永远别返回 null）；图标每次缝合都要重新注册；别把 MTE 塞进 GT6 的机器块（元数据会撞车）；物品显示名用 IMTE_GetItemName 最稳。
- **诊断方法**：客户端/服务端双端打点（readFromNBT2/onTick 的 remote 标记）一跑就能区分“服务端有、客户端无”类问题。
